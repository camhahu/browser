{
    "name": "browser",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run --filter ./packages/cli build",
        "test": "bun run --filter ./packages/cli test",
        "test:cli": "bun run --filter ./packages/cli test"
    }
}
